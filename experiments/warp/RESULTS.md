# Semantic Warp — RESULTS (W0–W4)

**Дата:** 2026-07-02
**Дизайн:** `docs/FRONTIER_SEMANTIC_WARP_2026-07-02.md`
**Код:** `experiments/warp/` (~1 100 LOC поверх существующего; ни один инвариант Phase 1–4 не тронут — warp-слой только читает состояние памяти и аннотирует M*-события)

Варп W\* — не пятая стадия Q/R/M/C, а provenance-предикат на M\*:
W\*\_θ := M\* ∧ (φ ≥ θ), где φ — доля чужой массы в тех же весах,
которыми merge выбрал кандидата (peer/centralized: trust·log1p(support),
CSM: trust·e^(−α·age)·conf, shared: массы наблюдений). φ замораживается
в момент lock'а (§3.1 дизайна) — собственное подтверждение по дороге
не размывает варп.

---

## W0 — Инструментация provenance → φ → W\* : **PASS**

`warp_instrumentation.py` + `warp_runner.py` + `exp_warp_anchor.py`.

- **Regression-якорь misled-A** (сценарий exp_4way_walk, peer K=4):
  lock агента A на (3,8) в tick 7 помечен **φ = 1.000, strict W\***,
  единственный источник — agent-C. ✓
- **Independent-контроль:** ровно 0 W\*-событий (strict и soft). ✓
- Каждый M\*-лог несёт: `phi`, `w_star_strict/soft`, `warp_radius_cells`,
  `warp_radius_visited`, `source_snapshot_age`, `co_recipients`,
  `co_locked`, `completed`, `rollback_latency`.
- Для peer/centralized per-source веса реконструируются **точно** из
  `AgentContribution` (trust, local_support), которые `local_merge` уже
  экспортирует — патч peer_memory не понадобился (читаем, не пишем).

Артефакты: `tmp/warp/anchor/anchor_results.json`.

## W1 — Counterfactual warp gain (masking sweep): **готово, знаки зафиксированы**

2 400 эпизодов: {(3,2),(5,3),(8,5),(4,4),(6,2)} × 3 layout ×
{peer-K2, peer-K8, CSM-K8, shared} × 20 сидов × mask on/off,
hazard 0.05. Маска = обнуление foreign-вклада на merge при том же сиде
и планировщике → WG(episode) = t_cens(masked) − t_cens(full) —
контрфактическая, не корреляционная атрибуция.

**H-W1 — ПОДТВЕРЖДЕНА (сильно).** P(C\*|W\*) ≪ P(C\*|M\*, own) при
scarcity, CI не пересекаются во всех архитектурах (единица — lock-событие):

| Архитектура | P(C\*\|W\*soft) [CI] | n | P(C\*\|M\*, own) [CI] | n |
|---|---|---|---|---|
| peer-K2 | 0.004 [0.001, 0.008] | 1 437 | 0.223 [0.186, 0.275] | 2 738 |
| peer-K8 | 0.020 [0.000, 0.038] | 149 | 0.389 [0.329, 0.446] | 1 734 |
| CSM-K8 | 0.017 [0.006, 0.044] | 637 | 0.414 [0.368, 0.453] | 1 684 |
| shared | 0.004 [0.002, 0.008] | 2 099 | 0.279 [0.237, 0.332] | 2 250 |

Вторая часть H-W1 (разрыв у CSM меньше, чем у fixed-K) — **частично**:
CSM держит P(C\*|W\*) выше peer-K2 (0.017 vs 0.004, CI не
пересекаются), но сопоставимо с peer-K8. Честно фиксируем.

**H-W2 — знак меняется по режиму, немонотонность подтверждена
частично.** Значимые ячейки: shared|random|ρ=1.5: **+10.8** тиков
[+2.2, +20.9] (варп помогает при unpredictable layout + умеренном
дефиците); peer-K2|random|ρ=1.67: **−7.1** [−13.5, −1.3] и
peer-K8|random: −5.3…−4.2 (fast-share варп вредит). При жёстком
дефиците ρ=3.0 эффект ≈ 0. Оговорка: в symmetric/asymmetric layout
размещение вод детерминировано по сидам (свойство env_factory), поэтому
статистический вес несут random-ячейки.

**H-W5 — понижена до exploratory.** Spearman(co_locked, 1−C\*|W\*)
= +0.033, p = 0.018 (n = 5 117 warp-событий): знак предсказанный,
значимость есть, но объяснённая дисперсия пренебрежима. Как «главный
предиктор» гипотеза не выжила; замена меры (co_locked вместо структурно
константного co-recipients = N−2) корректна и зафиксирована.

**Lock-цепочки: «варп как такси» (разрешение нестыковки W1).**
Кажущееся противоречие — shared|random даёт WG = +10.8 тиков при
P(C\*|W\*) = 0.004 — разрешается механизмом цепочек: W\*-lock
проваливается (цель занята), но перевозит агента в правильный регион,
где тот самоподтверждает воду и завершает под собственным re-lock'ом.
φ заморожен в момент lock'а, поэтому completion честно уходит в
own-страту. Постобработка существующих логов
(`analyze_warp_chains.py`):

| arm | own C\* | из них warp-assisted | r(первый W\*) → r(own) |
|---|---|---|---|
| shared\|random | 248 | 127 (**51.2%**) | 9.5 → 2.0 |
| peer-k2\|random | 223 | 99 (**44.4%**) | 9.7 → 2.0 |
| csm-k8\|random | 279 | 13 (4.7%) | 5.8 → 2.0 |
| peer-k8\|random | 246 | 7 (2.8%) | 8.4 → 2.0 |

Завершающий own-lock ставится в среднем с радиуса 2.0 = obs_radius —
точка самоподтверждения. Полная ценность варпа = прямые completion
(P(C\*|W\*)) + warp-assisted own completion; вторая компонента
доминирует у fast-share архитектур.

**Область контрфактуала.** Основная маска W1 — system-level (foreign
обнулён у всех агентов: ценность канала целиком). Per-agent
counterfactual реализован флагом `mask_agents` и промерен на
N5M3|random|peer-K2 × 20 сидов (`exp_warp_gain_individual.py`):
индивидуальный WG_i = **−10.25** [−20.91, −0.16] тиков (сырой варп
значимо вредит самому агенту на fast-share scarcity), spillover на
немаскированных агентов ≈ 0 (−0.62 [−3.06, +1.91]); знак согласуется
с системным контрфактуалом.

**Стратификация по K (данные для фигуры 1 и rebuttal главной
статьи).** Отдельный peer-sweep K ∈ {1,2,4,8,16} × scarcity-ячейки ×
random × 20 сидов (`make_warp_figures.py`, кэш
`strata_byK.json`): warp share падает 0.554 → 0.105 при K=1→8 при
почти постоянной P(C\*|own) ≈ 0.26–0.34 и P(C\*|W\*) ≤ 0.04 — 
C-collapse при малых K концентрируется в W\*-страте (механистическое
чтение bottleneck-shift). Отмечен честный wiggle: при K=16 share
возвращается к 0.23.

Артефакты: `tmp/warp/w1_gain/{w1_rows.jsonl, w1_summary.json,
w1_chains.json, w1_individual.json, strata_byK.json}`.

## W2 — Закон дальности (свидетель–путник): **PASS (все 4 вердикта)**

`exp_warp_age_law.py`. Гейт: age_max(trust) = ln(trust·conf/τ)/α,
conf = 0.95, α = 0.05, τ = 0.30.

**W2a — гейт-кривая (существование кандидата):**

| trust | эмпирический age_max | предсказанный | попадание |
|---|---|---|---|
| 1.00 | 23 | 23.05 | ✓ |
| 0.80 | 18 | 18.59 | ✓ |
| 0.60 | 12 | 12.84 | ✓ |
| 0.40 | 4 | 4.73 | ✓ (floor(pred) = emp) |
| 0.25 | гейт закрыт | 0 | ✓ trust-flip |

- **Trust-flip (§4.3):** trust ≤ τ/conf **обнуляет** W\*-события —
  дискретный выключатель подтверждён.
- **Peer (fixed-K): гейта нет** — кандидат жив при age 0…120 на всех
  trust. Различие архитектур на уровне ФОРМЫ кривой, как предсказано.

**W2b — completion-порог (полные эпизоды навигации).** Дискретное
уточнение неравенства осуществимости: путник сам видит воду за
obs_radius клеток, поэтому чужой гейт должен дожить до тика
t_gate = d − obs_radius − 1, а порог по начальному возрасту
bp = age_max − t_gate; в pruning-ячейке K=2 отсечка срабатывает на
broadcast-тиках: bp = 10K − t_bcast. Все 11 CSM-ячеек в допуске
(примеры: trust 1.0 d=6: emp 20 / pred 20.1; d=12: emp 14 / pred 14.1;
**pruning-ячейка K=2: emp 18 / pred 18 — тик-в-тик**, §4.4
предсказание о конкуренции pruning с гейтом подтверждено). Кривые —
монотонные ступени; у peer порога нет (успех при всех age до 36).

**Hold-out (out-of-sample): 6/6 EXACT.** Дискретное уточнение
формулировалось после осмотра исходной сетки W2b, поэтому проведён
hold-out (`exp_warp_age_law_holdout.py`): 6 новых ячеек (комбинации
d/trust/K, отсутствующие в исходной сетке), предсказания записаны на
диск **до** запуска эпизодов, age-сетка шага 1 (точнее исходной):

| ячейка | binding | предсказано | эмпирически |
|---|---|---|---|
| K8, trust 0.9, d=9 | gate | 14 | **14** |
| K8, trust 0.7, d=9 | gate | 9 | **9** |
| K8, trust 0.5, d=15 | gate | никогда (−1) | **−1** |
| K4, trust 1.0, d=6 | gate (pruning slack) | 20 | **20** |
| K2, trust 1.0, d=12 | **pruning бьёт гейт** | 12 | **12** |
| K2, trust 0.8, d=8 | gate (pruning slack) | 13 | **13** |

Все шесть — точные целочисленные попадания, включая ячейку «никогда»
и ячейку, где модель обязана выбрать pruning-границу (12) вместо
экспоненциальной (14.05) — и выбирает.

**H-W3 — ПОДТВЕРЖДЕНА, закон предсказывающий, не подогнанный.**
CSM-правила действительно управляют формированием intention.

Артефакты: `tmp/warp/w2_age_law/{w2_results.json,
holdout_predictions.json, holdout_results.json}`.

## W3 — Warp Drive protocol: **PASS (все 3 вердикта)**

`warp_drive.py` (optimistic lock + reservation + anti-M\* rollback,
~200 LOC) + `exp_warp_drive.py`. Резервации ездят на той же каденции K
полем снапшота (без центра); приоритет (reserved_at, distance,
agent_id); anti-M\* ретрагирует lock с exponential backoff
(4→8→…→32, hysteresis) и tie-break; TTL 4K против мёртвых владельцев.

- **Декомпозиция без центра:** тест «убери runtime» — протокол,
  управляемый прямыми вызовами слотов, принимает решения только из
  собственного inbox каждого агента. ✓
- **Misled-A разрешён превентивно:** с WD агент A вообще не делает
  обречённый lock на (3,8) — резервация C приходит раньше; A завершает
  на (8,7). Rollback latency обречённых lock'ов в sweep'е конечна и
  ≈ K: **1.0 / 1.5 / 2.7 тиков при K = 1 / 2 / 4** (у baseline —
  «до конца эпизода»). ✓
- **H-W4 — ПОДТВЕРЖДЕНА** (840 эпизодов, 20 сидов). Recovery считается
  против структурного потолка дефицита ceiling = min(m, M)/m (никакой
  протокол не создаёт лишнюю воду), а не против K=8:

| K | base P(C\*\|M\*) | + Warp Drive | ceiling | recovery |
|---|---|---|---|---|
| 1 | 0.605 [0.584, 0.627] | 0.638 [0.617, 0.662] | 0.641 | **0.90** |
| 2 | 0.597 [0.570, 0.624] | 0.709 [0.665, 0.761] | 0.649 | **2.15** |
| 4 | 0.629 [0.597, 0.661] | 0.926 [0.868, 0.988] | 0.674 | **6.6** |

  Recovery > 1 при K ∈ {2, 4}: подавление резервациями снижает само
  число обречённых M\*-lock'ов, поднимая условную вероятность выше
  потолка «все залочились».

**Безусловные метрики (контроль эффекта знаменателя).** Условная
P(C\*|M\*) сравнивает разные популяции lock'ов (lock census под WD
действительно падает: 0.944 → 0.739 при K=4), поэтому claim проверен
на безусловных величинах из тех же 840 эпизодов:

| arm | success rate [CI] | t_cens (fail=120) [CI] | lock census |
|---|---|---|---|
| K=1 base | 0.592 [0.574, 0.608] | 55.3 [53.7, 57.2] | 0.986 |
| K=1 +WD | **0.620** [0.609, 0.629] | 53.1 [52.0, 54.4] | 0.985 |
| K=2 base | 0.572 [0.549, 0.592] | 57.2 [55.1, 59.7] | 0.973 |
| K=2 +WD | **0.614** [0.601, 0.624] | **53.2** [52.0, 54.6] | 0.926 |
| K=4 base | 0.577 [0.554, 0.598] | 57.3 [55.1, 59.8] | 0.944 |
| K=4 +WD | **0.620** [0.609, 0.630] | **52.2** [50.9, 53.7] | 0.739 |
| K=8 base | 0.583 [0.558, 0.604] | 55.2 [52.9, 57.9] | 0.682 |

Success rate растёт под WD при каждом K с непересекающимися CI;
цензурированное время падает (K=2, K=4 — CI не пересекаются);
`mean_t_succ` только по успевшим у WD чуть выше — это survivor
composition (WD доводит и маргинальных агентов), поэтому опорная
метрика времени — цензурированная. **WD при любом fast-K превосходит
baseline K=8 по безусловному success rate (0.614–0.620 vs 0.583, CI
не пересекаются)** — claim «протокол снимает главный аргумент против
fast share» держится безусловно, а не только через условную
вероятность.

Артефакты: `tmp/warp/w3_drive/w3_results.json`.

## W4 — Cross-session варп на LLM-подложке: **PASS**

`exp_warp_cross_session.py`, Ollama llama3.1 локально ($0 API),
детерминированный кэш. `NoHintTextNavEnv` — из стокового TextNav
удалён направляющий оракул («the apple is west from here»): дальнее
знание доступно только из памяти.

- **Сессия 1:** агент-A обходит дом, LLM-экстрактор тегирует 27 мест
  из NL-наблюдений → persistence dump.
- **Сессия 2:** агент-B (свежая среда, ни одной собственной
  наблюдённой клетки) загружает дамп A как foreign provenance и решает
  «принеси яблоко на стол». Каждый его lock на якорные места —
  **φ = 1.0, strict W\*** (пример канонической раскладки: lock кухни
  tick 0 → take_apple tick 2, выбор самой LLM → lock стола tick 3 →
  put_apple tick 7 → C\* = 1).

**Репликация: 10 раскладок** (позиции яблока/стола/старта
рандомизированы по комнатам):

| | success | mean t_succ | завершённый strict W\* |
|---|---|---|---|
| warp (память A) | **10/10** | 8.3 тика | **10/10 раскладок** |
| control (пустая память) | **0/10** | — | — |

Контраст 10/10 против 0/10 — варп несущий, не декоративный: без
чужой памяти задача в no-hint среде является слепым поиском и не
решается за 30 тиков ни разу.

Насколько нам известно, это первый задокументированный **строгий W\*
через границу сессии и границу агента на LLM-подложке**: всё знание
о целях имеет provenance «agent-A, session 1». Атрибуция completion
использует аффорданс-радиус среды (take/put действуют при
manhattan ≤ 1, и свидетель тегирует якорь и на within-reach клетках —
lock соседней клетки, завершившийся успешным take/put, исполнил ровно
то, что обещал чужой evidence).

Инженерные факты стыка LLM↔символьная память (важно для roadmap
Этапов 4–5): (а) LLM query former свободно генерит required-теги вне
словаря памяти ('house') — нужен any-of гейт + короткие
retrieval-формулировки («Find the apple.»); (б) локальные 4–8B модели
надёжны в семантических решениях (какое место преследовать, когда
take/put) и ненадёжны в row/column-арифметике — исполнение разделено
по слоям стека songlines (Intent → Target → **Waypoint → Action**):
M-lock у LLM, локомоция — детерминированный waypoint-примитив.

Артефакты: `tmp/warp/w4_cross_session/w4_results.json` (полная трасса).

---

## W5a — Портируемость на непрерывную динамику (VMAS): **PASS (3/3 зарегистрированных)**

`exp_warp_vmas.py`, VMAS 1.5.2, тот же continuous→grid мост, что в
portability-тесте Claim 1. Предсказания зарегистрированы до прогонов
(`w5_registered.json`, `w5_predictions.json`):

- **P1 (страты):** P(C\*|W\*) = 0.012 [0.000, 0.042] против
  P(C\*|M\*, own) = 0.562 [0.457, 0.709] при K=1 — порядок величины,
  CI не пересекаются, на непрерывной динамике. ✓
- **P2 (share):** warp share монотонно падает 0.723 → 0.366 → 0.000
  при K = 1 → 4 → 16. ✓
- **P3 (метрический закон):** witness–traveler в непрерывном
  пространстве; t_sight калибруется одним прогоном a₀=0 (сворачивает
  скорость/drag/дискретизацию в одну константу), предсказание
  bp = ⌊age_max − t_sight⌋. Ячейка D=0.45/trust=1.0: emp 6 vs pred 4
  (в допуске сетки шага 2 — остаточная инерция); все три ячейки
  «за метрическим радиусом варпа» (D=0.75; trust=0.6) — предсказано
  «никогда», эмпирически «никогда», точно. ✓

Континуальная находка: инерция — новый физический канал утечки
варпа (агент докатывается в зону видимости после закрытия гейта);
закрыт тормозящим контроллером «нет lock'а — гаси скорость»
(континуальный аналог грид-семантики), отклонение задокументировано.

Артефакты: `tmp/warp/w5_vmas/`.

## W6 — Полноценный LLM-эксперимент (N=3, peer, K-sweep): **PASS (4/4 зарегистрированных)**

`exp_warp_llm_full.py`: мультиагентный no-hint text-мир, N=3 LLM-агента
(llama3.1 локально), M=2 яблока (scarcity), peer-broadcast place-дампов
с каденцией K ∈ {2, 8}, послойное исполнение (LLM — семантика,
exploration/локомоция — моторные примитивы). 4 раскладки × 2 K.

- **A1:** строгие W\* (φ=1.0) происходят на LLM-подложке. ✓
- **A2:** warp share падает с K: **0.417 при K=2 → 0.083 при K=8** —
  та же форма, что в гридах (0.554→0.105) и на VMAS (0.723→0.000). ✓
- **A3:** warp-коллизии при быстрой каденции (7 из 8 эпизодов). ✓
- **B (закон на LLM-тегах):** c берётся **измеренным** у экстрактора
  (c_measured = 0.900), age_max = ln(trust·c/τ)/α; все 4 ячейки в
  1 тике от предсказания (18/19, 16/17, 8/9, 6/7 при сетке шага 2). ✓

**Бонус — сырой варп вредит и на LLM.** Парное сравнение с
independent-baseline (те же раскладки/сиды, broadcast выключен):
без обмена 2/3 успеха в каждом эпизоде, с обменом — 1/3. Warp
collision съедает больше, чем даёт информация — H-W1/H-W2 паттерн
воспроизводится на языковой подложке; Warp Drive на LLM — очевидный
следующий шаг.

Инженерные находки стыка: (1) bootstrap-проблема — LLM-петле нужен
исследовательский ярус (аналог Tier-2 грид-планировщика), иначе
открытие не происходит и варпу нечем питаться; (2) экстрактор
галлюцинирует якорные теги на пустых клетках (например 'apple' в
холле) — шум словаря, который стратификация просто измеряет.

Артефакты: `tmp/warp/w6_llm_full/` (registered predictions + results).

## W6-C — Warp Drive на LLM-подложке: **PASS (3/3 зарегистрированных)**

`exp_warp_llm_drive.py`: тот же `WarpDriveProtocol` (без изменений),
та же сетка 4 раскладки × K ∈ {2, 8}, армы WD on/off.

- **C1:** pooled success 0.333 → **0.417** при обоих K (подъём в
  каждом K, layout L0 выигрывает целого агента). ✓
- **C2:** rollback latency конечна и ≈ K: почти все откаты за 1 тик
  при K=2, за 7 при K=8 — та же граница, что в гридах. ✓
- **C3:** коллизии не растут (7/7 эпизодов; метрика считает lock'и до
  подавления). ✓

Арка «вредит → чиним» замкнута на языке: independent 0.667 → сырой
обмен 0.333 → +Warp Drive 0.417. Честно: восстановление частичное
(~25% провала против ~90–100% в гридах) — на LLM-подложке к коллизиям
добавляются потери семантического уровня (шумные локи, галлюцинации
тегов), которые резервациями не лечатся. Отдельный разрез для R&D.

## R0 — Route-warp инфраструктура + smoke: **PASS (3/3 зарегистрированных)**

`exp_route_warp_r0.py` (по дизайну `FRONTIER_ROUTE_WARP_2026-07-02.md`):
comb-лабиринт с измеренным detour-фактором, `RoutePeerMemory`
(edge-снапшоты СВОЕГО опыта на каденции K, тот же privacy-контракт),
φ_route из тех же гейт-весов, BFS-follower по чужим рёбрам.

- **A1:** detour-фактор D = **2.65** (BFS 45 рёбер vs манхэттен 17). ✓
- **A2:** строгий **RW\***: φ_route = 1.0, 45 чужих рёбер пройдено,
  C\* = 1, t_succ = 54. ✓
- **A3:** route-арм (54 тика) против place-арм — place **вообще не
  дошёл за 300 тиков**. ✓

Бонус-находка сильнее ожидания R1: в мире с высоким detour place-warp
хуже слепого поиска (blind exploration дошёл за 126, place — никогда):
манхэттен-притяжение к известной цели прижимает планировщик к стенам,
тогда как разведка хотя бы метёт пространство. «Знать ГДЕ» без
«знать КАК» — активно вредно в лабиринте; песня-как-маршрут (route)
против песни-как-точки (place): 54 vs ∞. Регистрируемая гипотеза R1
(gain(D) с negative-control при D=1) получает априорную поддержку.

Артефакты: `tmp/warp/{w6_llm_full/w6c_results.json, r0_route/}`.

## R1 — Ablation gain(D): **обрыв вместо наклона (2 registered FAIL с механизмами, суть сильнее регистрации)**

`exp_route_warp_r1.py`: 4 корзины по ИЗМЕРЕННОМУ detour-фактору
(D=1.00 / 1.38 / 2.03 / 3.32), 20 сидов, парный дизайн (три арма на
идентичном лабиринте), step_limit 300.

| D | gain route-vs-place [CI] | gain route-vs-blind [CI] | succ route/place/blind |
|---|---|---|---|
| 1.00 | +4.7 [+3.4, +6.0] | +62 [+44, +80] | 20/**20**/20 |
| 1.38 | +281 [+279, +283] | +113 [+77, +155] | 20/**0**/16 |
| 2.03 | +273 [+271, +275] | +123 [+91, +160] | 20/**0**/17 |
| 3.32 | +262 [+259, +264] | +110 [+72, +152] | 20/**0**/15 |

- **R1-neg (D=1) — формально FAIL по тикам (+4.7 > 3), подтверждён
  в правильной метрике:** разрыв длин путей (moves) = **0.0
  [0.0, 0.0]** — тиковый гэп это экономия поворотов (BFS ходит
  прямыми отрезками, dominant-axis планировщик переключает оси на
  диагоналях), знание маршрута при D=1 не добавляет ничего. Дизайн-
  замысел negative-control выполнен; метрика в R2+ — клетки, не тики.
- **R2 — формально FAIL (монотонность), суть сильнее:** place-warp
  не деградирует с D, а **коллапсирует бинарно на первой стене** —
  cliff локализован между D=1.00 и D=1.38 (пробная корзина D15):
  place success 1.0 → 0.0, place-vs-blind переворачивается
  +57.5 → −168. Gain насыщается на цензурирующем потолке (place
  никогда не финиширует: lock-притяжение к стене — ловушка-цикл, не
  медленность) и потому не может «расти» с D.
- **Exploratory-патология из R0 подтверждена систематически:** при
  всех D ≥ 1.38 place-warp хуже слепого поиска.

Честный итог R1: «знать ГДЕ» без «знать КАК» перестаёт работать при
первом же нетривиальном препятствии, а песня-как-маршрут доводит
всегда (20/20 во всех корзинах, все — строгие RW\* с φ_route=1.0).
Правильная форма закона — ступень по D, не наклон; формулировка R2
для статьи переписывается как cliff-detection + success-rate ablation.

Артефакты: `tmp/warp/r1_route_gain/`.

## R2 — Разрыв маршрута по старению: **PASS (3/3, точность до ребра)**

`exp_route_warp_r2.py`. Свидетель штампует рёбра последовательно
(ребро i в тик i) — след стареет неравномерно, начало маршрута всегда
старше конца, и опоздавший путник **гонится за угасающим следом**.
Гейт тот же (trust·e^{−α·age}·log1p(count) ≥ τ); binding-ребро — 
текущее (старейшее из оставшихся). Предиктор детерминированный:
калибровка без распада записывает кинематику (индекс пути по тикам,
включая повороты), предсказание — прогон замкнутой формы гейта по
этой траектории (тот же ход, что дал 6/6 hold-out в W2). Предсказания
зарегистрированы до эпизодов.

5 лабиринтов (L = 10–14 рёбер) × a₀ ∈ {0..10} × trust ∈ {1.0, 0.7}:

- **P1 (режимы complete / rupture@i\* / DOA): 110/110 (100%).** ✓
- **P2 (позиция разрыва): все 21 mid-route разрыва — ошибка 0 рёбер**
  (предсказанный индекс = эмпирический, без единого отклонения). ✓
- **P3 (trust-flip для маршрутов):** при trust=0.7 масса одного
  прохода (log1p(1)=0.693) даёт age_max=9.6 < L → **ни один маршрут
  не закоммитился** — дискретный выключатель, теперь для путей. ✓

Патология «песня оборвалась на полуслове» из дизайна — это RW\*-lock
с C\*=0 и измеренной позицией обрыва; позиция предсказывается заранее
до ребра. Закон дальности обобщён с мест на пути: у place-варпа гейт
закрывался в момент, у route-варпа он закрывается **в точке маршрута**,
и обе координаты выводятся из одних и тех же констант (α, τ, trust).

Артефакты: `tmp/warp/r2_route_rupture/`.

## W7 — Семантическое тождество мест (frame-free warp): **PASS (3/3, v2)**

`semantic_identity.py` + `exp_warp_semantic_identity.py`. Ответ на
вопрос «как агенты понимают, что имеют в виду одно и то же место»:
до W7 — только общий координатный кадр (семантика лишь подтверждала
внутри радиуса). W7 инвертирует: **отпечатки мест** (локальные
созвездия hazard/wall/границ на относительных смещениях,
translation-invariant), **взаимно-уникальный матчинг** с точным
порогом, восстановление сдвига кадров по ≥3 согласным парам
совместно посещённых ориентиров, транспорт чужого evidence — включая
никогда не виденную путником воду — через восстановленный сдвиг.
Механика песни буквально: общие ориентиры якорят незнакомую часть.

Свидетель и путник в разных приватных кадрах (секретный offset),
2 арма × 4 offset × 2 плотности hazard × 10 сидов:

- **P1 (координатный baseline fails open):** при любом ненулевом
  offset лочит не-воду в 100% прогонов; завершает только случайным
  наступанием (6.7% — путь к смещённой цели пересекает настоящую
  воду). ✓
- **P2 (семантический арм):** восстановление offset **точное в 100%**
  (все смещения × сиды, в среднем 15.1 взаимных пар), completion
  100%, все локи — строгие W\* (φ=1.0). ✓
- **P3 (aliasing-мир, h=0, полоса без ориентиров):** семантика делает
  **0 lock'ов** — fails closed; координаты при ненулевом offset —
  100% ложных lock'ов — fails open. Два режима тождества ломаются в
  противоположные стороны: отказ vs уверенная ошибка. ✓

Ревизия v1→v2 задокументирована в registered-файле: (а) fail-open
операционализирован через ложный lock, а не случайные завершения;
(б) матчер v1 принимал однонаправленно-уникальные пары — далёкие
чужие клетки алиасились в полосу получателя (60% точности);
v2 = взаимная уникальность + точный порог + ≥2 salient-ключей → 100%.

Ограничение (заявлено): только трансляция кадров (у грида абсолютная
ориентация через действия); rotation-invariant отпечатки — future
work. `songline_drive/place_identity.py` (HYBRID) остаётся
одиночно-агентным; W7 — первый коллективный слой семантического
тождества.

Артефакты: `tmp/warp/w7_semantic_identity/`.

## W8 — Семантическое тождество в полном peer/CSM-стеке: **PASS (4/4)**

`semantic_peer_memory.py` (`SemanticFramePeerMemory`) +
`exp_warp_semantic_stack.py`. Семантическое тождество W7 становится
полноценной мультиагентной памятью: N агентов, у каждого приватный
кадр (секретные несовпадающие offsets), broadcast с каденцией K,
**выравнивание развивается онлайн** — по мере накопления совместно
посещённых ориентиров пер-отправительский сдвиг кадра
восстанавливается посреди эпизода; до консенсуса память fails closed.
CSM-гейт trust×staleness — ортогональный слой поверх транспорта.

**Part A** (N=4, M=2, random+hazards, 3 арма × K∈{2,8} × 20 сидов):

| арм | pooled success |
|---|---|
| oracle (общий кадр, как вся основная серия) | 0.456 |
| coordinate при несовпадающих кадрах | 0.350 |
| **semantic при тех же кадрах** | **0.444** |

- A1: semantic > coordinate, CI не пересекаются. ✓
- A2: восстановлено **88%** разрыва oracle−coordinate. ✓
- A3: strict W\* (φ=1.0) функционируют через восстановленные кадры
  (прямые completions + такси-цепочки > 0; v2-ревизия
  задокументирована: доля прямых W\*-completions при scarcity
  управляется W1-феноменологией коллизий, не восстановлением кадров). ✓
- Выравнивание сработало в **100% эпизодов**, средний onset — тик 14.5.

**Part B — замыкание серии: закон дальности не заметил исчезновения
общей сетки.** Свидетель и путник в разных кадрах (offset (5,−3)),
CSM-гейт поверх семантического выравнивания, сетка возрастов шага 1:

| ячейка | предсказано (форма W2) | эмпирически |
|---|---|---|
| trust 1.0, d=6 | 20 | **20** |
| trust 1.0, d=12 | 14 | **14** |
| trust 0.6, d=6 | 9 | **9** |

Тот же закон, те же константы, те же целые точки — но между агентами
больше нет ни одной общей координаты. Гейт и восстановление кадров —
ортогональные слои, как и предсказывалось.

Методологическая гигиена: протокол Part B прошёл три исправления
загрязнений, все задокументированы (полоса разметки видела воду →
собственный evidence шунтировал чужой гейт; слепой доход с
самоподтверждением получал lock; финальная семантика — **непрерывный
lock от foreign-коммита (φ=1 в момент первого lock'а) до успеха** —
это каноническая семантика completion всей серии).

Артефакты: `tmp/warp/w8_semantic_stack/`.

## W9 — Ротационно-инвариантные отпечатки (SE(2)): **4/5 PASS + 1 честный FAIL с механизмом**

`align_frames_se2` (4 гипотезы поворота × провалидированный
трансляционный aligner + правило доминирования ≥2×) +
`SemanticFramePeerMemory(aligner='se2', frame_rotations=…)` +
`exp_warp_rotation.py`. Приватные кадры теперь различаются и секретным
поворотом (0/90/180/270°), и трансляцией — общих осей больше нет.

- **P2 — ядро: 80/80.** SE(2)-кадр (r\*, δ\*) восстановлен **точно во
  всех 80** комбинациях (4 поворота × 2 смещения × 10 сидов),
  completion 100% со строгим W\* (непрерывный lock, φ=1.0). ✓
- **P3 — принципиальная нераспознаваемость.** В 180°-симметричном мире
  кадр невосстановим в принципе: se2 видит две равные гипотезы и
  **отказывается (0/10 локов)**; координатный арм на тех же кадрах —
  10/10 ложных локов. Fail closed vs fail open, теперь по оси
  ориентации. ✓
- **P4-v2 — полный стек под случайными SE(2)-кадрами.** Success
  нечувствителен (повёрнутый координатный яд улетает за сетку и
  деградирует в псевдоразведку — v1-регистрация опровергнута и
  задокументирована); различающая метрика — **точность warp-локов**:
  semantic **1.000** (8/8 строгих W\* на настоящей воде) против
  **0.000** (0/992 фантомных) у координат; onset выравнивания 100%. ✓
- **B — закон дальности не заметил и поворота:** bp = 20 точно при
  witness rotation 90°. ✓
- **P1 — registered FAIL, правда острее регистрации:** трансляционный
  aligner НИКОГДА не восстанавливает кадр под поворотом (0/60), но не
  «всегда fails closed»: 58/60 отказов и **2/60 fail open** через
  180°-симметричную подструктуру hazard'ов — ровно тот механизм,
  который мотивирует правило доминирования se2 (на тех же сидах se2
  точен).

**Corner-trap и его лечение (важно для методов):** в полном стеке
агенты стартуют из углов, а границы прямоугольного мира
180°-симметричны — ранние void-отпечатки кормили неверную
180°-гипотезу (точность семантики была 0.159 до правки). Принцип:
**границы — свойство формы мира, а не места**; ориентир обязан
содержать ≥1 содержательный тег (void-only отпечатки исключены из
матчинга). После правки: W7 3/3, W8 4/4 (регрессии), W9 точность 1.000.

Кадровых допущений больше не осталось: ни общего origin (W7/W8), ни
общего севера (W9) — только общая семантика ориентиров и правило
«отказывайся, когда мир не даёт различить».

Артефакты: `tmp/warp/w9_rotation/`.

## W10 — Абляция словаря ориентиров: **PASS (4/4) — обеднение стоит покрытия, но не корректности**

`exp_warp_landmark_ablation.py`: словарь Σ (какие классы тегов
допускаются в отпечаток) сам становится воздействием. Миры со стенами
И hazard'ами, свидетель/путник как в W7, 7 словарей × 3 смещения × 12
сидов (252 ячейки), без планировщика — исход = само выравнивание и
перенесённый таргет.

- **A1 — fail-safe абляция: 0 fail-open из 252.** Любой перенос при
  любом словаре идёт через точно восстановленный кадр; обеднение
  превращает recovery в отказ, никогда — в ложный лок. Асимметрия
  закрытых отказов выдерживает не только неоднозначные миры (W7/W9),
  но и обеднённые алфавиты. ✓
- **A2 — void — леса, а не идентичность:** 0.000 массы консенсусных
  ключей; удаление ничего не меняет (recovery 1.00 = full). Согласуется
  с corner-trap W9. ✓
- **A3 — одного содержательного класса достаточно:** hazard-only
  recovery 1.00; wall-only 0.83 (остальное — отказ); void-only — 0
  переносов. Соответствию нужно ≥3 взаимно-уникальных констелляций над
  каким-нибудь content-классом — бюджет ограниченной рациональности. ✓
- **A4 — атрибуция измерима:** hazard_edge 0.557 / wall 0.443 / void
  0.000 массы ключей консенсусных пар при полном Σ. ✓

Смысл для серии: «какие признаки работают ориентирами» — измеримое
свойство пары мир×словарь, а не удачное модельное допущение; открытый
хвост — механизм, который *отбирает* Σ из координационного успеха
(словарь как результат координации, а не дизайна).

Артефакты: `tmp/warp/w10_landmark_ablation/`.

## R3 — Hazard-стратификация: **PASS (3/3) — песня не только ведёт, она бережёт**

`exp_route_warp_r3.py`. Открытое поле с hazard-плотностью 0.20
(проходимые, но наказуемые клетки), свидетель ходит безопасным путём
(BFS без hazard'ов, L = 10–14 рёбер), 20 миров, парный дизайн.
Ось безопасности изолирована от оси старения (S1/S2 без распада).

| арм | hazard hits [CI] | completion | время |
|---|---|---|---|
| route (чужой безопасный путь) | **0** (во всех 20) | 20/20 | 16.6 |
| place (только «где») | **3.25** [2.45, 4.10] | 20/20 | 15.9 |
| blind | 28.70 | — | — |

- **S1 — перенос безопасности:** маршрут наследует hazard-избегание
  свидетеля целиком (0 хитов всюду); place-путник знает «где», но
  манхэттен-линия ведёт сквозь поле — 3.25 удара за эпизод. ✓
- **S2 — оси независимы:** completion 100% у обоих армов — в
  hazard-мире place-warp не проваливается по завершению (в отличие от
  стен R1), он проваливается по цене. Цена безопасности у route —
  всего +0.7 тика. Стены → completion-ось, hazard'ы → safety-ось. ✓
- **S3 — обрыв песни возвращает риск (композиция с R2):** в
  rupture-режиме (19/20 миров) до разрыва — **0 хитов**, после
  разрыва (fallback на place-знание) — 0.58 [0.11, 1.32], CI не
  включает 0; **все 19 разрывов — точно на R2-предсказанном индексе
  ребра (ошибка 0)** — рёберный предиктор реплицирован на новом классе
  миров бесплатно. ✓

**Route-фронтир R0–R3 закрыт полностью.** Итоговая стратификация того,
что переносит песня: place-знание («где») — цель; route-знание
(«как») — completion в стенах (R1: cliff у place на первой стене) и
безопасность в hazard'ах (R3: 0 vs 3.25); старение обрывает перенос в
предсказуемой точке (R2: 110/110, ошибка 0 рёбер), и с обрывом
возвращается риск (S3).

Артефакты: `tmp/warp/r3_hazard/`.

## Definition of done (дизайн §11)

- [x] Каждый M\*-лог несёт φ и warp-поля; misled-A помечен строгим W\*
- [x] Карта warp gain по (layout, ρ) с CI; знаки H-W1/H-W2 зафиксированы честно, включая слабые/негативные
- [x] Кривая P(C\*|W\*)(age) с breakpoint-тестом; таблица age_max воспроизведена
- [x] Warp Drive измерен против baseline; rollback latency конечна (≈K); децентрализация подтверждена тестом «убери runtime»
- [x] Cross-session strict W\* на LLM-подложке с C\* = 1 — реплицирован на 10 раскладках (10/10 vs контроль 0/10)
- [x] Инварианты Phase 1–4 не нарушены (warp-слой read-only; planner не переписан; Warp Drive действует через тот же targets-интерфейс)

## Отклонения от дизайна (зафиксированы честно)

1. **Tie-break резерваций** расширен до (reserved_at, distance,
   agent_id): при одновременных lock'ах чистый agent_id отдавал цель
   дальнему агенту в ущерб её первооткрывателю в двух клетках.
2. **Recovery H-W4** считается против потолка дефицита min(m, M)/m,
   а не против K=8: разница между структурным (нехватка воды) и
   координационным (коллизии) коллапсом.
3. **W2b-порог** использует дискретное уточнение (горизонт
   самоподтверждения obs_radius + расписание pruning) — константы
   закона (α, τ, trust) не менялись.
4. **H-W5**: co_locked (операциональная мера давления) вместо
   co-recipients как таковой: co-recipients в broadcast-архитектурах
   структурно константен (N−2), вариацию несёт co-lock.
5. **W4**: исполнение действий разделено по слоям — LLM принимает
   семантические решения (M-lock, take/put), локомоция к залоченной
   цели — детерминированный waypoint-примитив (llama3.1 ненадёжна в
   координатной арифметике; моторный навык не является предметом W4).
   В deliver-фазе LLM и сама шла верно — примитив исправлял только
   fetch-направление.

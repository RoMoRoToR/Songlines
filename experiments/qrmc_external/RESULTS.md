# Q/R/M/C external-framework validation — RESULTS

**Дата:** 2026-07-04
**Модель:** llama3.1 (Ollama, temperature 0), 96 эпизодов: 3 фреймворка
(OpenAI SDK tool-loop, LangGraph ReAct, AutoGen classic) × 4 варианта ×
8 сидов, идентичные эпизоды/инструменты/бюджеты, единый tool-trace
контракт (`memory_house.py`).

## Семантика событий (v3, история ревизий в registered.json)

- Q\* — агент запросил память (≥1 recall);
- R\* — атрибуция: выдача содержит запись, упоминающую предмет И
  указывающую на истинное место;
- M\* — эпизодный lock на истинное место (верно Definition 1);
  M1 (первый commit) и stale_first — вспомогательные метрики качества
  lock'а.

## Таблица (8 сидов на ячейку)

| framework | variant | Q | R | M | C | M1 | stale1 |
|---|---|---|---|---|---|---|---|
| openai_sdk | control | 1.00 | 1.00 | 0.75 | 0.75 | 0.12 | — |
| openai_sdk | r_starved | 1.00 | **0.00** | 0.38 | 0.38 | 0.12 | — |
| openai_sdk | m_ambiguous | 1.00 | 1.00 | 0.88 | 0.12 | 0.12 | 0.38 |
| openai_sdk | c_budget | 1.00 | 1.00 | 0.12 | **0.00** | 0.12 | — |
| langgraph | control | 1.00 | 1.00 | 0.75 | 0.50 | 0.50 | — |
| langgraph | r_starved | 1.00 | **0.00** | 0.38 | 0.25 | 0.12 | — |
| langgraph | m_ambiguous | 1.00 | 1.00 | 0.62 | 0.62 | 0.38 | 0.38 |
| langgraph | c_budget | 1.00 | 1.00 | 0.12 | **0.00** | 0.12 | — |
| autogen | control | 1.00 | 1.00 | 0.88 | 0.38 | 0.38 | — |
| autogen | r_starved | 1.00 | **0.00** | 0.50 | 0.25 | 0.12 | — |
| autogen | m_ambiguous | 1.00 | 1.00 | 0.38 | 0.25 | 0.12 | 0.62 |
| autogen | c_budget | 1.00 | 1.00 | 0.12 | **0.00** | 0.12 | — |

## Выводы

1. **Контракт портируется без адаптации метрики**: одни определения
   на tool-trace всех трёх фреймворков.
2. **Структурные отказы локализуются одинаково всюду**: консолидационный
   провал → R=0.00 (3/3 фреймворка), бюджетный → C=0.00 при Q=R=1
   (3/3). Эта часть P2 держится идеально.
3. **Headline: чистая задача разъезжается 0.38–0.75 по success при
   одной модели** — и декомпозиция атрибутирует: первокоммитная
   дисциплина слаба везде (M1 0.12–0.50, первый goto игнорирует
   собственную выдачу), различие фреймворков — в петле восстановления
   (openai_sdk чинится: M 0.75→C 0.75; autogen лочит, но не доводит:
   M 0.88, C 0.38).
4. **Registered-провалы честно**: P1-контроль предполагал чистый
   control (≥6/8 по всем стадиям) — сломан слабостью коммита самих
   фреймворков; m_ambiguous C-деградация не отделяется от деградир.
   контроля при n=8; дистрактор работает (stale_first 0.38–0.62,
   у AutoGen 0.62 — сильнее всех тянется к протухшей записи).

Ограничения: одна модель (llama3.1), n=8 на ячейку, три фреймворка;
Letta/CrewAI требуют Python ≥3.10 (venv 3.9). Артефакты:
`tmp/qrmc_external/{registered.json, rows.json, summary.json}`.

## Scale-up 2026-07-05/06: n=20 × 2 модели

**llama3.1 n=20** (таблица в статье обновлена): структурные ячейки
идеальны (R=0.00, C=0.00 в 20/20 на 3/3 фреймворках); control-разброс
0.50–0.75, M1 0.25–0.50; ambiguity-цена framework-специфична
(openai_sdk 0.75→0.30, остальные поглощают).

**qwen3:4b n=20 (P3 — зарегистрирован до прогонов в
registered_scaleup.json): PASS точно.** Структурные локализации
перенеслись без отклонений (R=0.00 / C=0.00, 20/20, 3/3). Поведенчески
qwen радикально лучше llama: control C=1.00 всюду, M1=0.90–0.95
(против 0.25–0.50), stale-отвлекаемость 0.05–0.30 — КРОМЕ AutoGen,
роняющего qwen-M1 до 0.40. Декомпозиция разделяет три влияния, которые
лидерборд смешивает: структурные пределы задачи
(модель/фреймворк-независимы), качество коммита модели, и
prompt/loop-шейпинг фреймворка, способный его разрушить.
Think-off для qwen (бюджетный паритет) зафиксирован в регистрации.
Артефакты: rows_{llama20,qwen20}.json, summary_{llama20,qwen20}.json,
p3_verdict.json.
